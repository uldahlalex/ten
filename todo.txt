Aktuelt:

appsettings prob:
- test setup defaulter til appsettings.json

server app
- tests

client app
      - create list, tag, task, update any of these

hvis tunit test runner aldrig bliver fikset så migrate til andet

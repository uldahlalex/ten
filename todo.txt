Aktuelt:

deno

server app
      - create tag, list
      - update tag, list
      - task update TEST
      - tag update TEST
      - list update TEST

client app

      - create list, tag, task, update any of these
